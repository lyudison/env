ENV_DIR=$PWD
ln -s -i $ENV_DIR/zshrc ~/.zshrc
# TODO: Use VSCode instead of vim now
# ln -s -i $ENV_DIR/vimrc ~/.vimrc
ln -s -i $ENV_DIR/tern-config ~/.tern-config
ln -s -i $ENV_DIR/ctags ~/.ctags
ln -s -i $ENV_DIR/ycm_extra_conf.py ~/.ycm_extra_conf.py
ln -s -i $ENV_DIR/tmux.conf ~/.tmux.conf
